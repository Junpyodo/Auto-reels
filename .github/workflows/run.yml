name: Auto Reels Uploader (2 Times a Day)

on:
  workflow_dispatch:
  schedule:
    # 하루에 두 번 실행 (한국 시간 12:00 PM, 12:00 AM)
    - cron: '0 3,15 * * *' 
  repository_dispatch:

jobs:
  build-and-run:
    runs-on: ubuntu-latest
    # 한 번 성공하면 나머지 시도는 취소하도록 설정
    strategy:
      fail-fast: true
      matrix:
        attempt: [1, 2, 3]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg imagemagick fonts-noto-cjk
          # ImageMagick 보안 정책 수정 (자막 생성을 위해 필수)
          sudo sed -i 's/domain="path" rights="none" pattern="@\*"/domain="path" rights="read|write" pattern="@\*"/g' /etc/ImageMagick-6/policy.xml

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          # moviepy 버전 고정 (ModuleNotFoundError 해결)
          pip install moviepy==1.0.3 openai requests

      - name: Run Reels Script
        env:
          INSTAGRAM_ACCESS_TOKEN: ${{ secrets.INSTAGRAM_ACCESS_TOKEN }}
          INSTAGRAM_ACCOUNT_ID: ${{ secrets.INSTAGRAM_ACCOUNT_ID }}
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 메인 스크립트 실행
          python main.py

      - name: Commit and Push Changes
        if: success()
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          # 변경된 파일(사용된 대본, 추가된 주제)을 저장소에 업데이트
          git add used_scripts.json topics.txt
          git commit -m "🚀 Update logs and topics [skip ci]" || echo "No changes to commit"
          git push origin main